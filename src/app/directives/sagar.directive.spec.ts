import { SagarDirective } from './sagar.directive';

describe('SagarDirective', () => {
  it('should create an instance', () => {
    const directive = new SagarDirective();
    expect(directive).toBeTruthy();
  });
});
